services:
  # Core PHP App Application
  app:
    user: "${DOCKER_UID}:${GID}"
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_app
    restart: unless-stopped
    tty: true
    volumes:
      - ./src:/var/www
    networks:
      - laravel_network

  # Web Server (Using Built-in PHP server targeting the /public directory)
  webserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_web
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./src:/var/www
    working_dir: /var/www
    command: php artisan serve --host=0.0.0.0 --port=8000
    depends_on:
      - app
    networks:
      - laravel_network  

  # MariaDB Database
  db:
    image: mariadb:11
    container_name: laravel_db
    restart: unless-stopped
    ports:
      - "3307:3306"
    environment:
      MARIADB_DATABASE: web4jobs_db
      MARIADB_ROOT_PASSWORD: root_password
      MARIADB_USER: web4jobs_user
      MARIADB_PASSWORD: user_password
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - laravel_network

  # Redis Cache / Queue
  redis:
    image: redis:alpine
    container_name: laravel_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - laravel_network

  # External Auth Mock Service
  auth_mock:
    image: node:alpine
    container_name: laravel_auth_mock
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ./auth-api.js:/app/auth-api.js
    working_dir: /app
    command: node auth-api.js
    networks:
      - laravel_network

networks:
  laravel_network:
    driver: bridge

volumes:
  dbdata:
    driver: local